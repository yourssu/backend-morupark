apiVersion: apps/v1
kind: Deployment
metadata:
  name: queue-service
  namespace: morupark-prod
spec:
  replicas: 5
  template:
    spec:
      containers:
      - name: queue-service
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1000m"