apiVersion: apps/v1
kind: Deployment
metadata:
  name: goods-service
  namespace: morupark-prod
  labels:
    app.kubernetes.io/name: goods-service
    app.kubernetes.io/component: goods
    app.kubernetes.io/part-of: morupark-msa
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: goods-service
      app.kubernetes.io/component: goods
  template:
    metadata:
      labels:
        app.kubernetes.io/name: goods-service
        app.kubernetes.io/component: goods
        app.kubernetes.io/part-of: morupark-msa
    spec:
      serviceAccountName: goods-service-sa-prod
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
        - name: goods-service
          image: asia-northeast3-docker.pkg.dev/yourssu-morupark/morupark-repo-private/goods-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8083
              protocol: TCP
          env:
            - name: SERVER_PORT
              value: "8083"
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: DB_URL
              value: "jdbc:mysql://127.0.0.1:3306/moruparkdb_prod?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul"
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: morupark-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: morupark-gcp-secrets
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: morupark-gcp-secrets
                  key: DB_PASSWORD
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms512m -Xmx1024m -XX:MaxMetaspaceSize=256m -XX:+ExitOnOutOfMemoryError"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 120
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
        - name: cloud-sql-proxy
          image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.8.0
          args:
            - "--structured-logs"
            - "--port=3306"
            - "$(SQL_INSTANCE_CONNECTION_NAME)"
          env:
            - name: SQL_INSTANCE_CONNECTION_NAME
              valueFrom:
                configMapKeyRef:
                  name: morupark-sql-config
                  key: SQL_INSTANCE_CONNECTION_NAME
          securityContext:
            runAsNonRoot: true
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
