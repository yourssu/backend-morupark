apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: morupark-prod
spec:
  selector:
    app.kubernetes.io/name: api-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
